import React from 'react';

const BotSections = () => {
    return (
        <div className={"card mx-4 px-3"}>
            Buttons(will add later)
        </div>
    );
};

export default BotSections;
